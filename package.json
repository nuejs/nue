{
  "name": "nue",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1",
    "node": ">=20"
  },
  "jest": {
    "setupFilesAfterEnv": ["jest-extended/all", "<rootDir>/setup-jest.mjs"],
    "testEnvironment": "@happy-dom/jest-environment"
  }
}
